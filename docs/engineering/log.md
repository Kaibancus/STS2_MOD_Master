# 工程日志

日志采用追加记录。计划是意图，日志是已发生的事实；禁止将未来任务写成已完成。
每条实施记录关联任务/BUG ID、变更、依据、实际验证、未覆盖项和交接。
修正历史记录时追加更正说明，不悄悄抹去此前事实。

本文件不存放真实存档、账户目录、私人路径、完整游戏分析输出或未经脱敏的日志。

## 2026-09-05：初始化基线

关联：INIT-01。

已完成：将游戏副本、MOD 仓库和普通存档备份分为独立本地目录；
建立公开仓库及 `main`；初始化说明、忽略规则和源码/资产占位。
只备份普通档位相关数据，不包含现有其他 MOD 的存档。

可公开证据：初始化内容提交
`dd55bd3f585b549dea38d4017813f32874afebe5`。
复制与存档完整性证据留在本地备份目录，不提交其私人源路径或快照清单。

未做：没有角色实现、C# 工程或运行探针，没有游戏启动、
存档恢复和 Steam 云同步设置变更。

## 2026-09-05：研究与需求规划

关联：R-01、R-02、D-01。

用户确认：首期为一个可玩的单人角色，联机和创意工坊后置；
使用仓库内中文 Markdown 管理 PRD、计划、日志和双看板；
角色设计文件初始为真正空文件，内容由用户后续填写。
必要时允许仅本地参考的程序集分析，禁止上传游戏代码和资产。

已完成的静态工作：确认游戏 `v0.111.0`、GodotSharp `4.5.1`、
`net9.0` 和 Harmony `2.4.2`；检查随附 XML 与 PE/CLI 元数据，
识别原生装载/模型/池接口、Manifest 字段、角色资源扩展限制、
命令/钩子和存档隔离风险。

公开资料已整理完成，包括官方公告、社区固定版本源码、stock Godot 和
MegaDot 的分发、校验、PCK 与导出模板区别。用户进一步确认原生接口优先，
按探针引入必要依赖，并选择官方 MegaDot 4.5.1-m.14 C# 便携包作为候选编辑器。

当前状态：文档草案仍处于规划评审阶段，编辑器尚未下载；
这里没有记录“文档已在仓库发布”或“Godot 已安装”。
实施落地后应追加实际交付和安装结果，再更新看板。

未做：没有执行游戏代码，没有启动游戏，没有安装 Godot、
分析工具或 MOD 依赖，没有修改仓库的正式角色设计。

## 后续实施记录字段

每个批次追加一个条目，至少包含：实际日期、关联任务/BUG、目标、
实际修改、来源或设计依据、已执行验证及结果、未执行项、风险/偏差、
实际提交或可定位证据、交接和看板状态变化。

任务开始与状态变化记入看板；批次结束必须有日志。
如果工具失败后修正重试，应如实记录对工程结果有影响的偏差，
不得将中途失败隐去后声称首次即成功。

## 2026-09-05：批准文档落地

关联：D-01、D-02、D-03、D-04；D-05、E-01 尚待后续结果。

依据：用户已批准文档与候选编辑器准备批次，首期单人角色、原生接口优先、
兼容探针先于固定版本库或窄 Harmony 补丁，联机及创意工坊后置。
本条承接前面的规划记录，规划时的“尚未下载/发布”不是当前状态快照。

实际修改：按批准稿原样转入 13 份中文 Markdown，再仅修正文档批准状态、
已交付任务状态和持续维护要求。建立 PRD、技术评估、来源表、阶段计划、
索引、工程日志及双看板。`AGENTS.md` 要求后续批次同步维护计划、任务、
BUG/FIX 与追加式日志，并禁止覆盖用户后续填写的角色设计。
`.gitignore` 只新增根目录 `/Godot/`；保留既有游戏、存档、隐私保护、
`.gitattributes` 和 `src` / `assets` 占位文件。

已执行：转入时逐份比对批准稿与目标文件，13 份全部字节一致；
状态修订后核对 25 个本地链接、31 个任务 ID、14 个 FR、8 个 NFR、
6 个验收门、18 项风险及 28 个公开/本地来源 ID，未发现未定义引用。
角色设计文件为 0 字节；BUG/FIX 表无虚构记录；
未来安全、角色设计、接口探针及业务任务仍为待办或阻塞。
文档与原创源码、图片、音频路径可跟踪，游戏/存档/本机路径仍被忽略，
`/Godot/` 仅限制仓库根目录。

状态：D-01 至 D-04 已完成本地文档交付；本条写入时尚未提交，
D-05 等待编辑器实际准备结果及最终发布审查。E-01 由独立的仓库外准备工作负责，
这里不代替其声明下载、版本响应或自包含模式成功。

未执行：未运行游戏、引擎、构建或 MOD 探针，未安装依赖，
未恢复存档或修改 Steam Cloud；没有创作角色设定、机制、数值或内容配额。
后续追加实际工具结果及公开提交证据，不要求本条引用自身提交。

## 2026-09-05：候选编辑器准备结果

关联：E-01、D-05；不代表 S-01/S-02 或 A-01 至 A-07 已完成。

仓库外准备工作已完成并交回实际结果。官方来源为 S06/S07 所指的
`megadot-4.5.1-m.14-windows-x86_64-llvm-editor-csharp.zip` 及其 `.sha256`，
完整下载为 `578164427` 字节，实际完整 SHA-256 为
`bd42d2c8e41653b451a1ee714447c333c738a515ad2f554673b8960a364e5829`，
与官方校验值匹配，没有使用镜像或绕过校验。

解压前已审查归档路径；48 个供应方文件共 `2335698504` 解压字节，
准备在 `<workspace>\Godot\megadot-4.5.1-m.14-win64-csharp`。
可执行文件为 `MegaDot_v4.5.1-stable_mono_win64.exe` 及其 `_console.exe` 变体；
原生 ProductVersion 为 `4.5.1.m.14.mono.custom_build`。
首次调用前创建零字节 `_sc_`，仅运行 console `--headless --version`，
未指定项目，退出码 0，版本输出为 `4.5.1.m.14.mono.custom_build`。

明确未覆盖：版本查询后 `editor_data` 不存在，只能认定自包含标记已配置；
没有验证完整编辑器设置/缓存行为、.NET/NuGet/SDK 隔离、资源导入、
PCK 或游戏兼容性。没有打开编辑器 UI/项目或游戏，没有安装额外 SDK、
框架或导出模板，没有修改全局配置、Steam Cloud 或存档。
本机准备凭据、原始输出和下载临时文件均在仓库外处理，不随文档提交。

过程偏差：初始 curl 整包和分段传输较慢，改用内置 PowerShell HTTP 客户端，
在同一官方地址进行有界四工作器分段顺序传输；逐段核对 Content-Range 和长度，
连续拼接后核对完整包哈希。不将下载过程调整登记为虚构的项目 BUG。

状态：E-01 以“候选包准备”范围完成，G1/G2 仍未通过，
角色设计仍为 0 字节；D-05 已解除等待并进入最终文档审查、提交和发布。
本条写入时尚未提交，实际发布证据将在发生后追加，不预造提交哈希。

## 2026-09-05：文档基线发布

关联：D-01、D-02、D-03、D-04、D-05、E-01。

实际内容提交：`d52cec1203580390d927f91dff72e0f6da996770`，
已从现有 `main` 非强制推送到公开仓库
`https://github.com/Kaibancus/STS2_MOD_Master`。
推送后已确认该提交等于远端 `main`，本地跟踪 `origin/main`，当时工作区干净。
本条是事后确认记录，引用已存在的内容提交，不引用自身尚未产生的哈希。

内容范围：13 份批准 Markdown 文档及 `.gitignore` 的单条 `/Godot/` 保护。
仓库共 17 个跟踪文件；原有 `.gitattributes`、源码和资产占位保持不变。
已审查完整暂存内容及可达发布历史，没有游戏/参考程序集、工具包、
原始分析输出、真实存档、账户目录或私人准备凭据。

文档核对覆盖 25 个本地链接、31 个任务 ID、14 个 FR、8 个 NFR、
6 个验收门、18 项风险、28 个来源 ID、表格结构及状态依赖。
`docs/character-design.md` 在工作区和提交中均为 0 字节，未填入任何创作内容；
BUG/FIX 看板没有制造缺陷记录。没有为文档安装新的构建、格式化或测试依赖。

状态：D-01 至 D-05 与 E-01 已完成本批范围，G0 文档与来源基线已具备；
G1/G2 及其后续运行验收仍未通过。U-01 等待用户创作输入，
安全边界、接口探针、角色实现及发布产物任务仍保持待办或阻塞。
E-01 仅完成候选包准备与版本查询，不能推导完整自包含隔离或游戏兼容成功。

## 2026-09-05：存档安全批次与操作系统边界探针

关联：S-01、S-02、NFR-01。用户要求开发副本运行不能覆盖复制来源的存档，
并授权提供受保护的持久开发入口；本批不涉及角色实现。

已领取任务并开始目标版本的只读静态分析。操作系统能力由独立只读检查确认：
Windows Sandbox 功能已启用，Hypervisor 可用，当前会话非管理员；
没有为本批启用系统功能、修改权限或切换账户。

在首次游戏运行前，经确认仅执行 Sandbox 边界探针：
只读映射 `<workspace>\game` 到客体 `C:\Sts2Game`，
读写映射新建独立 `<workspace>\dev-data` 到 `C:\Sts2Data`，
除此之外没有宿主目录映射。禁用网络、剪贴板、音视频输入和打印机重定向，
启用 ProtectedClient；本次禁用 vGPU。

实际结果：客体对游戏映射的写入被拒绝，IP-enabled 网络适配器为 0，
未发现 Steam 进程或标准安装目录；客体写入的专用标记已在宿主开发数据目录观察到。
客体随后自行关机，确认无遗留 Sandbox 或游戏进程。本次未启动游戏。
独立复核源 AppData、游戏专属 Steam 缓存及原安装的完整基线，
文件内容、修改时间及路径集合均未变化；私人清单与客体结果不进入 Git。

自身入口目前具备物理路径/重解析点、持久目录归属、硬链接、程序集指纹、
系统能力与并发实例拒绝检查；默认只预检，不启动。生成配置与开发数据均在仓库外，
不复制真实存档、不恢复备份、不修改云设置；不会自动退回宿主直接执行游戏。
自身脚本检查不需要安装依赖，客体脚本已通过 Windows PowerShell 5.1 语法检查。

未覆盖：这次只证明映射与网络边界，不能宣称游戏成功初始化、实际保存路由
或跨启动续用已通过。S-01 仍待原生链结论；S-02/G1 仍待经确认的真实存储验证。
当时所有改动未提交；后续以追加记录补充实际运行结果或阻塞。

## 2026-09-05：按用户要求重新评估独立离线存档

关联：S-01、S-02。

用户要求优先将开发存档作为独立离线存档，不再默认采用较重的 Sandbox 路线。
已暂停该路线的收尾、发布与任何真实游戏启动；保留自身未提交实现和已经发生的
边界验证记录，没有删除既有工作或恢复源存档。确认无本批遗留的 Sandbox/游戏进程。

重新评估的判定条件：目标版本必须确实不构造/使用 Steam Cloud 后端，
并同时将设置、进度、当前局、历史及 MOD 数据写到独立的持久开发路径。
不能只因网络关闭或平台名变成默认值就通过 G1；原有默认离线存档也受保护。
当前等待原生静态链的具体证据，不猜测参数、不修改认证检查或全局环境。

## 2026-09-05：离线路由实验证据

关联：S-01、S-02、BUG-001。

静态链已核实 `--force-steam=off`、Steam 初始化与 CloudSaveStore 构造条件、
默认离线 ID=1、Windows APPDATA getter 与发行配置的 `SlayTheSpire2` 自定义目录。
同时确认根级/账号级迁移、普通至 modded 复制与云本机缓存均不能仅靠“关闭云同步”
规避。必要方法和实际 EXE 证据见 L07/L08；没有向 Git 复制游戏 IL、配置或资源。

经确认，两次在既有禁网 Sandbox 边界内执行真实游戏，固定参数
`--headless --force-steam=off`，只对子进程设置 APPDATA、LOCALAPPDATA、TEMP、TMP。
首次创建设置、偏好和进度三份真实存档；第二次同根启动到达主菜单，
三份文件的长度和 SHA-256 均保持一致，没有重新生成它们。
两次均观察到跳过 Steam 初始化，没有启用 CloudSaveStore 的日志，
未在客体默认 AppData 建立游戏目录。日志及相关 Sentry 本地数据也进入专用根。
独立对照确认两次运行后全部原 AppData、游戏专属 Steam 缓存和原安装
的文件集合、长度、修改时间及内容哈希不变。

随后经代码审查与明确确认，使用自身原生入口在宿主执行一次 60 秒无界面验证，
专用根为 `<workspace>\offline-data\v0.111.0-41cef1ea\main`。
约 11.2 秒到达主菜单，实际写入 `default\1\settings.save`、
`profile1\saves\prefs.save` 和 `progress.save`，日志确认跳过 Steam 初始化。
APPDATA/LOCALAPPDATA/TEMP/TMP/USERPROFILE/PATH 在调用方进程、用户和机器
三个作用域的前后值一致。宿主源数据的独立复核在后续条目记录。

这些运行均在限时后只停止自身创建的游戏 PID，退出码 -1 是主动停止结果，
不是自然正常退出；每个 Sandbox 随后自行关闭，未遗留游戏或 Sandbox 进程。
第二次 guest 及首次 host 的无界面 stderr 出现 `Invalid Task ID`，
如实保留为上游运行观察；没有修改游戏，也不宣称玩法/图形/退出流程无错误。

实现：日常入口 `tools\Start-OfflineDev.ps1` 不依赖 Sandbox，
固定参数且不接受任意附加参数，子进程专用环境，全发行 220 文件指纹，
版本/slot 数据隔离，路径/数据硬链接/并发与注入环境拒绝。
`-Launch` 的全量指纹核对位于已持有互斥的临界区；默认仅预检且不创建数据。
无界面诊断显式限时，普通 `-Launch` 不带 headless；没有自动启动的 build/pack。
没有实现备份自动导入；默认新空档，备份始终不是运行目录。

自身工具复验：52 项无依赖检查覆盖固定参数、子进程环境不影响父进程、
版本输入增删/变化、非法路径/slot、重解析点、硬链接、已有根和跨进程互斥；
另有实际发行预检。修复了 BUG-001 的函数作用域/导出错误，错误发生时入口未启动游戏。
这些只证明已列范围，不证明防御恶意宿主并发篡改或任意不可信 MOD。

工具过程偏差：为必要原生核对，在现有分析工具缺失时从官方 NuGet 获取固定
Iced 1.21.0，仅放在仓库外使用，没有作为 MOD 依赖或升级 SDK。
但 `dotnet nuget verify` 的首次 CLI 调用输出了首次运行初始化和
“已安装 ASP.NET Core HTTPS 开发证书”提示。这是非预期环境副作用，
已通知协调会话；未检查/删除证书，也没有擅自回滚用户证书存储。
不能将本批笼统描述为“完全零环境副作用”；后续工具应预先限制 first-run 行为。

本条写入时实现尚未提交。S-01 完成；S-02 等待宿主源基线独立确认及最终发布记录。

## 2026-09-05：宿主源数据独立复核

关联：S-02、BUG-001。

协调会话在获准宿主运行结束后独立重新枚举并核对原 AppData、
游戏专属 Steam 缓存及原安装；各自文件集合、长度、修改时间和 SHA-256
均未变化，没有新增、删除或改写。结合两次 guest 同根持久性和一次 host
真实路由证据，确认无需追加游戏运行；后续没有再启动游戏。

S-02/G1 按已锁定的可信发行基线通过，53 项自身检查通过，
BUG-001 已复验关闭。未来装载、资源、角色和内容任务仍待实施批准或用户设计，
不能借用本次有限验收宣称新游戏版本、额外 MOD 或任意网络行为已兼容。
日常使用原生离线入口，默认新空档；源备份不参与运行，也未进行真实存档导入。

清理与保留：自身检查的唯一临时夹具均在 finally 中清理；
分析进程已停止。仓库外的局部分析结果、固定版本分析工具和自身读取器
作为本版本契约复核证据保留；guest/host 私有诊断与自建离线档为路由、持久性证据，
均不进入 Git。没有检查私钥材料、删除或清理证书；
此前 SDK 首次运行提示的副作用保留在记录中，不以存档安全结论掩盖。

当前源文件与文档即将提交发布，实际公开提交与远端确认在发生后追加，
不预造哈希或修改此前日志。

## 2026-09-05：离线入口发布

关联：S-01、S-02、BUG-001。

实际实现提交 `cf586fffc51e3af72646ba4d6b418917c8bc6dde`
已从现有 `main` 非强制推送到公开仓库。推送后远端 `main` 与本地提交一致，
工作区当时干净。本条事后记录引用已经存在的实现提交，不要求引用自身哈希。

本批发布只包含 14 个相关自身源码/文档路径，共 22 个跟踪文件；
没有游戏分发文件、派生 IL/ASM、分析工具包、私有诊断、证书材料或玩家数据。
原有角色设计文件仍为 0 字节；未更改源码/资产占位或文本属性规则。
局部分析工具、结果与自建运行数据留在仓库外，保留用途见前条。

S-01/S-02 与 BUG-001 已完成相应闭环；G1 仅针对当前锁定的可信发行基线，
不是任意 MOD 或新版本的通行证。G2 及后续角色/内容工作继续等待单独批准和设计输入。
E-01 的早期“不能通过 G1/G2”只表示编辑器准备本身没有这些证据，
不能覆盖本批 S-02 后来完成的事实；旧日志保留，当前看板明确区分两者。

## 2026-09-05：A-01 原生装载探针

关联：A-01、FR-01、NFR-01/02/06/08。用户仅批准技术装载入口，
不扩展到模型/池、角色、卡牌、遗物或其他 P2/P3 任务，角色设计仍为 0 字节。

实际实现：新增 `src\NativeLoaderProbe`，固定 SDK 9.0.317 / `net9.0`，
只引用外部 `sts2.dll` 且 `Private=false`。原始静态入口使用
`ModInitializerAttribute`，以 Interlocked 拒绝重复调用，并仅写一条自身日志；
技术 ID 为 `sts2modmaster_loader_probe`，版本 `0.1.0`，无 PCK/依赖/玩法影响。
没有引入 BaseLib、RitsuLib、Harmony 补丁、Godot 导出或创作占位。

构建/部署/运行分离：普通构建无复制到游戏或启动操作。受控构建使用子进程专用 CLI-home，
显式禁用遥测和开发证书生成，固定引用目录，不修改全局环境；没有安装新工具或包。
构建前采集源码/项目/SDK 配置/构建脚本指纹，编译打包后再次比对，
只将原先捕获的指纹写入凭据，避免并发编辑把新源码配给旧 DLL。
构建凭据和部署凭据仅在仓库外保存。

产物仅两份：自身 DLL 为 4608 字节，SHA-256
`3142A0B03CF51E1AA67E34A6755D423B27D05F1D93A53D484ED305987EC22B03`；
本次 Manifest 为 358 字节，SHA-256
`B4BBF4C99B5C573B6EC150592600C11FFD772B80E51119674017F6EE2CA97752`。
相同本地输入下两次强制重新编译得到相同 DLL；构建零警告/错误，没有游戏引用混入包中。

显式探针启动仍核对原 220 文件完整基线，只额外允许固定自身 DLL/JSON 路径，
并核对当前源输入、构建/部署凭据、实际哈希/长度、链接和并发。
默认无探针模式仍拒绝它们。83 项现有 PowerShell 辅助检查覆盖原有保护、
新增允许清单、缺失/篡改凭据和产物、构建期间输入变化、档位与阶段隔离及同意保护。
曾将构建与使用同一互斥的检查并行，检查按设计被锁拒绝；改为串行后通过，未放宽锁。

首次带探针运行前完成源码/产物/允许清单和同意方案审查，随后只运行一次：
`-LoaderProbe -Launch -ProbeSeconds 60`。游戏参数仍为
`--headless --force-steam=off`，子进程数据固定到独立 `loader-probe` slot。
首次只为该新档以 schema 8 创建原生 MOD 同意，不复制或改写 `main` 设置。

实际 stdout 按原生流程显示发现 Manifest、加载自身 DLL、调用
`Sts2ModMaster.NativeLoaderProbe.LoaderProbe`，并恰好一次输出
`STS2_MOD_MASTER_NATIVE_LOADER_PROBE/0.1.0 INITIALIZED count=1`。
之后显示初始化完成、`Loaded 1 mods (1 total)`，约 10.69 秒到主菜单。
Steam 初始化被跳过；数据进入该 slot 的 `default\1\modded\profile1`。
原生明确报告没有普通测试档可复制，跳过首次 modded 复制。
60 秒后只停止自身 PID，退出码 -1 为主动限时停止；没有自动重跑。
两条 `Invalid Task ID` 是单独保留的 headless 运行观察，不冒充 initializer 失败或完整玩法验收。

保护源核对：原 AppData、游戏专属 Steam 缓存、原安装及现有 offline-main
的全部文件集合与内容 SHA-256 均保持一致；AppData/原安装/main 的时间戳也不变。
唯一元数据例外是 Steam 缓存索引 `remotecache.vdf` 的最后写入时间刷新，长度和内容相同。
协调会话只读检查的 Steam 云日志明确记录另一个客户端的缓存评估、文件已同步及索引写回，
其时间与该索引刷新吻合，早于本次游戏诊断日志创建。
这记录的是独立客户端行为，不推断谁启动了客户端，也不把整个批次写成“零元数据变化”。
没有重设时间戳、修改缓存、恢复备份或放宽审查以消除这一观察。

收尾已用精确 `-Remove` 删除凭据匹配的自身两文件和专用空 leaf，
移除部署凭据并恢复开发游戏副本的 220 文件/原摘要；默认离线预检重新通过。
忽略的自身构建产物、仓库外构建凭据/专用 CLI 数据与新测试 slot/原始诊断保留用于复现；
没有将二进制、游戏方法输出、真实存档或私有路径清单提交到 Git。
所有辅助夹具已清理，没有游戏/Sandbox 遗留进程。

A-01 按真实原生调用与限定安全证据完成；A-02 至 A-07、完整 G2 和角色内容仍未完成。
本条写入时变更尚未提交，后续以实际提交历史定位本批源码和记录，不预造自身哈希。

## 2026-09-08：五角色、全卡牌和专属遗物离线报告

关联：REPORT-01、BUG-002、BUG-003。用户要求按当前安装版本整理五角色基础属性、
所有卡牌的类别/费用/稀有度数量及完整列表，随后补充所有角色专属遗物。
本批不是下一阶段角色实现，未填写零字节设计文档，未关闭 A-02 至 A-07 或完整 G2。

版本固定为本机实际 `v0.111.0 / 41cef1ea`，`sts2.dll` 和 PCK 指纹与既有基线一致；
不声称 9 月 8 日全球最新版本。原始 PE 类型库存独立对照运行结果：
Cards 命名空间 596 个直接 CardModel 加 8 个 Mocks 测试类型；
Relics 命名空间 299 个 RelicModel 加 1 个普通辅助类；
8 个角色类型、14 个卡池类型和 9 个遗物池类型全部逐项登记。

受控提取：自身 net9.0 诊断仅用外部 `sts2.dll` / `GodotSharp.dll` 引用，
使用已有固定 SDK、专用 CLI 数据与禁用首次运行/开发证书生成配置，没有安装新工具或依赖。
首次实际运行前提交精确源码、三个输入哈希、新报告映射及终止上限，逐次获得确认。
配置只映射只读独立开发副本、只读自身 DLL/Manifest/guest 脚本、
新的可写报告目录；禁网络、剪贴板、音视频输入和打印机，启用 ProtectedClient。
只在客体内部复制游戏并装入诊断，宿主游戏目录/通用启动信任/既有 loader probe 均未改变。
新报告档使用固定 `--headless --force-steam=off` 和子进程环境，未复用 main/loader-probe 档。

诊断等待 `NGame.GameStartupComplete` 后读取 canonical 白名单属性、原生模型集合和池成员，
写出事实文件后正常退出；没有调用 OnPlay、升级、战斗命令或修改 canonical 状态。
中文/英文名称由固定 PCK 中六张表的 `.title` 键静态读取后按模型 ID 精确关联；
完整表、效果正文、故事、图像和游戏方法体未进入公开输出。
生成/精炼关系只保留必要的模型引用、原生字典配对与方法标识。

失败与修正：第一版误执行 Deprived 测试池生成器，触发原生 test-only 断言，明确退出 1，
未输出成功数据，也没有开启 TestMode 或修改断言。第二版已排除明确的4个测试/废弃池，
但命名空间辅助类 VakuuCardSelector 被错当模型，泛型约束失败再次退出 1。
通过纯 PE 基类/构造器核对定位问题，第三版增加 AbstractModel 子类及 Mocks 分类检查，
保留完整库存，正常退出 0、非超时，写出完整事实。BUG-002 已复验关闭。
此外，源码编译期间的 Environment 名称歧义、nullable 和局部变量作用域错误均在运行前修正，
未以缺失数值或默认 0 绕过错误。

第三版结果：5 个原生可玩角色、596 个已注册卡模型、299 个已注册遗物模型；
去掉明确废弃占位后，报告为 595 张唯一卡、298 个唯一遗物。
11 个合法活动卡池共有 595 条不重复成员：角色直接池 454，共享/事件/状态等 141。
角色相关生成牌 9 张是附加归属维度，仍保留 TokenPool，不增加全局唯一数量。
8 个活动遗物池有 301 条成员关系，对应 298 个唯一遗物；
LastingCandy、RazorTooth、SparklingRouge 各有两条池成员关系，未重复计数。

五角色生命/金币/能量分别为铁甲 80/99/3、静默 70/99/3、储君 75/99/3、
亡灵契约师 66/99/3、故障机器人 75/99/3。
直接卡池分别 90/91/91/91/91；相关衍生牌分别 1/1/4/1/2。
每角色直接专属遗物池 8 个，原生 TouchOfOrobas 精炼字典明确另外 1 个初始遗物升级形态，
合计每角色 9 个归属遗物、全体 45 项。事件精炼形态没有被伪装为普通角色池掉落。
另行核对到 LastingCandy 的首次铁甲对局排除是资格条件，不是铁甲专属关系。

数值语义：能量 X 优先识别；Unplayable 关键字与上下文 IsPlayable 分开。
Clash、GrandFinale、HighFive 的条件 getter 需要玩家/可变模型，保留 null 和三个问题记录，
但它们的基准费用、类型、稀有度、目标均完整，未标成永久不可打出。
星费不混入能量费，Star X 与无星费 -1 区分；均值只用固定数字能量，含 0，并展示分母。
升级费用、独立初始星数及药水容量未证明，明确 null/未核实，不为可选字段追加游戏运行。

交付：`reports\sts2-v0.111.0-cards.html` 为真正独立的中文离线页面，
完整 595 卡/298 遗物均可搜索、按角色/归属依据/类别/稀有度/费用/来源筛选和排序，
包括当前筛选的数量、固定费用均值、星费分布及交叉表、初始牌组、排除与来源说明。
所有统计来自同一内嵌 JSON，不独立硬编码图表计数；同目录保留结构化事实数据和复现说明。
页面无外部资源、fetch、eval、CDN 或遥测，所有数据文本用 textContent，
JSON 内嵌对小于号、与号和行分隔符转义。

验证证据：6539 项数据检查涵盖库存/唯一 ID/池成员和重叠/完整短名称/数值类别/
初始清单/精炼关系/排除/HTML 内嵌一致性。
现有 Edge 独立临时配置通过 41 项实际 file:// 交互检查：
五角色直接与相关卡、8+1 遗物、组合筛选、空状态、重置、Clash 搜索、
X 费与数值费用升降序、注入文本不成 HTML、390px 无页面横向溢出以及打印表头/全量行。
打印时隐藏全部 button 导致表头消失的问题按 BUG-003 修复，print 中保留 th button；
目标 None 已用独立“无目标”标签，避免与遗物无等级混用。
首次 CDP 辅助脚本的 VoidTaskResult 返回混入已修正并重新通过，未安装浏览器框架。

协调会话独立核对了所有 595 卡的 ID/类别/稀有度/目标/原始能量与星费、
全部 298 遗物及完整 HTML 内嵌数据，也复核角色统计与固定费用分母。
提取期间用户正在宿主原 Steam 安装游玩，原 AppData/Steam 缓存出现正常运行变动，
不能将其作为不变快照或归因于报告；没有恢复或修改这些源文件。
报告客体没有原源/Steam/原安装映射且禁网，原安装及既有 offline-data 独立核对不变，
实际提取只访问只读的独立开发副本及新报告目录。
为此仅报告专用能力检查允许无关宿主游戏并存，并检查8GiB客体加宿主内存余量；
既有通用离线/loader/Sandbox启动保护完全未放宽，没有终止用户游戏。

本批原始失败/成功捕获、局部静态事实中间文件、CLI 数据及客体输入保留在仓库外作来源复核，
临时浏览器与辅助夹具已清理；不将它们提交为报告资产。
报告期间没有改动游戏/依赖基线、全局配置或证书。旧批次日志中的证书副作用仍按历史保留。
REPORT-01、BUG-002、BUG-003 完成；当前源码/数据/报告变更尚未提交，
后续实际提交历史即为本批实现定位，不预造哈希或改写旧日志。

发布前复核：只保留已经通过实际 Edge 检查的单套模板/生成器，
已确认归属的未采用 UI 草稿清除后重新生成，恢复到已审阅的 983324 字节 HTML，
其 SHA-256 为 `7FD9E081C95CF0FC14D8FC2947E062FD9ACD901E27BE299A3C8F645BED9D6D5D`，
再次确认内嵌数据与 JSON 完全一致。发布预检遇到既有网络代理不可连接，
仅在单次 Git 子进程禁用代理后直连成功，未改变仓库/全局代理或系统设置。
