# 项目实施约定

## 范围和安全

- 本仓库只存放可公开的自身代码、明确授权的资产和相关文档。
- 游戏、编辑器、参考程序集、提取/反编译结果、真实存档及本机配置位于仓库外。
- 不修改原 Steam 安装；任何游戏级调试先通过实施计划中的数据隔离门。
- 不假定复制游戏、切换档位或出现 `modded` 目录就已经隔离真实数据和云存储。
- 没有明确任务时，不升级工具链、不新增依赖、不引入遥测或自动发布。

## 设计权威

- `docs/character-design.md` 由用户提供创作内容，初始必须保持零字节。
- 未经用户要求，不填入标题、模板、角色设定、机制、数值或内容配额。
- 用户已填写后保留其内容，不得为恢复“初始零字节”状态而清空或覆盖。
- 设计缺失或规则有歧义时阻塞相关业务任务，不通过猜测绕过。
- 可丢弃的技术探针不是正式角色设计，不能算作正式内容完成。

## 每个实施批次的记录

1. 开始前阅读 PRD、技术评估、实施计划及相关设计，领取任务 ID 并更新任务看板。
2. 变更范围或技术路线时更新计划/决策依据，不只在聊天中说明。
3. 发现真实缺陷时维护 BUG/FIX 看板；将推测风险与已复现缺陷分开。
4. 批次结束追加工程日志，记录实际修改、版本、验证证据、失败或未覆盖项；更正也以追加条目记录，不覆写历史。
5. 更新任务状态和缺陷状态；没有验收证据不能标记完成或关闭。
6. 提交前检查文档链接、任务/BUG/需求 ID 和状态一致性，并审查发布边界。

使用真实已有提交或证据定位实现。当前改动尚未提交时明确写“未提交”，
不要伪造哈希，也不要要求一个提交在自身文件中引用自身哈希。

## 工程原则

- 原生装载、模型/池和命令接口优先；候选库和 Harmony 仅解决已证实缺口。
- 保持游戏版本、工具和依赖基线明确；公共教程不能代替本版本接口核对。
- 玩法状态与 UI/VFX 分离，正确处理 canonical/mutable 模型及订阅生命周期。
- 使用合适的确定性随机流；持久化模型/字段变更必须考虑旧档和缺依赖。
- 仅运行适合改动范围的已有验证；未执行或失败的步骤如实记录。
- 项目文档使用中文，代码符号和路径保持清晰稳定；不把本地私人信息写进公共文档。
